---
name: screenshot-design
description: Use when a user asks to run the Design OS /screenshot-design workflow to capture section screen design screenshots.
---

# screenshot-design

This skill is a Warp wrapper around the canonical command workflow in `.claude/commands/design-os/screenshot-design.md`.

## Execution rules

1. Read `.claude/commands/design-os/screenshot-design.md` fully before acting.
2. Follow that workflow exactly, including prerequisite checks, stop conditions, and required user-facing copy.
3. Keep `.claude/commands/design-os/screenshot-design.md` unchanged unless the user explicitly asks to edit it.
4. If this wrapper conflicts with the command file, the command file is the source of truth.
